# @package _global_

defaults:
  - override /models@model_spec: metasr
  # - override /datasets@dataset_spec: astgtmv003

dataset_spec:
  cache: in_memory

exp_name: ${dataset_spec.name}_${model_spec.encoder.name}-metasr
device: "cuda:0"

run_spec:
  runner:
    type: runner.train.v1

monitor_spec:
  loss: min
